const gameSettings = {
    boardSize: 14, // don't change
    gameTick: 200, // speed in miliseconds, ! value is game tick !
    increaseEvery: 50, // Increase speed every 50 points
    increaseValue: 20, // Значення на яке збільшується швидкість
};

export { gameSettings };
